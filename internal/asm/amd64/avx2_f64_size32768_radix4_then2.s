//go:build amd64 && !purego

// ===========================================================================
// AVX2 Size-32768 Radix-4-then-2 FFT Kernels for AMD64 (complex128)
// ===========================================================================
//
// This file contains a radix-4-then-2 DIT FFT optimized for size 32768.
// Stages:
//   - Stage 1-7: radix-4 (7 stages)
//   - Stage 8: radix-2 (final combine)
//
// Unlike the smaller radix-4-then-2 kernels, the mixed digit-reversal table
// is passed in as a slice argument (bitrev []int) instead of being embedded
// as a DATA table: at 32768 entries the table would add 256 KiB to the
// binary. The caller passes internal/kernels' shared table.
//
// Generated by a one-off script following the avx2_f64_size8192 pattern;
// hand-edit like any other kernel.
//
// ===========================================================================

#include "textflag.h"

TEXT ·ForwardAVX2Size32768Radix4Then2Complex128Asm(SB), NOSPLIT, $0-121
	// Load parameters
	MOVQ dst+0(FP), R8       // R8  = dst pointer
	MOVQ src+24(FP), R9      // R9  = src pointer
	MOVQ twiddle+48(FP), R10 // R10 = twiddle pointer
	MOVQ scratch+72(FP), R11 // R11 = scratch pointer
	MOVQ bitrev+96(FP), R12  // R12 = bitrev pointer
	MOVQ src_len+32(FP), R13 // R13 = n (should be 32768)

	// Verify n == 32768
	CMPQ R13, $32768
	JNE  m24_32768_return_false

	// Validate slice lengths
	MOVQ dst_len+8(FP), AX
	CMPQ AX, $32768
	JL   m24_32768_return_false

	MOVQ twiddle_len+56(FP), AX
	CMPQ AX, $32768
	JL   m24_32768_return_false

	MOVQ scratch_len+80(FP), AX
	CMPQ AX, $32768
	JL   m24_32768_return_false

	MOVQ bitrev_len+104(FP), AX
	CMPQ AX, $32768
	JL   m24_32768_return_false

	// Select working buffer
	CMPQ R8, R9
	JNE  m24_32768_use_dst
	MOVQ R11, R8             // In-place: use scratch

m24_32768_use_dst:
	// ==================================================================
	// Stage 1: 8192 radix-4 butterflies with radix-4-then-2 bit-reversal
	// ==================================================================
	XORQ CX, CX              // CX = base offset

m24_32768_stage1_loop:
	CMPQ CX, $32768
	JGE  m24_32768_stage2

	// Load bit-reversed indices
	MOVQ (R12)(CX*8), DX
	MOVQ 8(R12)(CX*8), SI
	MOVQ 16(R12)(CX*8), DI
	MOVQ 24(R12)(CX*8), R14

	// Load input values
	MOVQ DX, AX
	SHLQ $4, AX
	VMOVUPD (R9)(AX*1), X0
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R9)(AX*1), X1
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD (R9)(AX*1), X2
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD (R9)(AX*1), X3

	// Radix-4 butterfly
	VADDPD X0, X2, X4
	VSUBPD X2, X0, X5
	VADDPD X1, X3, X6
	VSUBPD X3, X1, X7

	// (-i)*t3 for y1
	VSHUFPD $0x1, X7, X7, X8
	VXORPD ·maskNegHiPD(SB), X8, X8

	// i*t3 for y3
	VSHUFPD $0x1, X7, X7, X11
	VXORPD ·maskNegLoPD(SB), X11, X11

	VADDPD X4, X6, X0
	VADDPD X5, X8, X1
	VSUBPD X6, X4, X2
	VADDPD X5, X11, X3

	// Store outputs to work buffer
	MOVQ CX, AX
	SHLQ $4, AX
	VMOVUPD X0, (R8)(AX*1)
	VMOVUPD X1, 16(R8)(AX*1)
	VMOVUPD X2, 32(R8)(AX*1)
	VMOVUPD X3, 48(R8)(AX*1)

	ADDQ $4, CX
	JMP  m24_32768_stage1_loop

	// ==================================================================
	// Stage 2: 2048 groups, each with 4 butterflies
	// Twiddle step = 2048
	// ==================================================================
m24_32768_stage2:
	XORQ CX, CX

m24_32768_stage2_outer:
	CMPQ CX, $32768
	JGE  m24_32768_stage3

	XORQ DX, DX

m24_32768_stage2_inner:
	CMPQ DX, $4
	JGE  m24_32768_stage2_next

	MOVQ CX, BX
	ADDQ DX, BX
	LEAQ 4(BX), SI
	LEAQ 8(BX), DI
	LEAQ 12(BX), R14

	// Twiddles: j*2048, 2*j*2048, 3*j*2048
	MOVQ DX, R15
	SHLQ $11, R15
	MOVQ R15, AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X8
	LEAQ (R15)(R15*1), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X9
	LEAQ (R15)(R15*2), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X10

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X0
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X1
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X2
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X3

	// Complex multiply a1*w1, a2*w2, a3*w3
	VMOVDDUP X8, X11
	VPERMILPD $1, X8, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X1, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X1, X13
	VMOVAPD X13, X1

	VMOVDDUP X9, X11
	VPERMILPD $1, X9, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X2, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X2, X13
	VMOVAPD X13, X2

	VMOVDDUP X10, X11
	VPERMILPD $1, X10, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X3, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X3, X13
	VMOVAPD X13, X3

	VADDPD X0, X2, X4
	VSUBPD X2, X0, X5
	VADDPD X1, X3, X6
	VSUBPD X3, X1, X7

	VSHUFPD $0x1, X7, X7, X14
	VXORPD ·maskNegHiPD(SB), X14, X14

	VSHUFPD $0x1, X7, X7, X12
	VXORPD ·maskNegLoPD(SB), X12, X12

	VADDPD X4, X6, X0
	VADDPD X5, X14, X1
	VSUBPD X6, X4, X2
	VADDPD X5, X12, X3

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD X0, (R8)(AX*1)
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD X1, (R8)(AX*1)
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD X2, (R8)(AX*1)
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD X3, (R8)(AX*1)

	INCQ DX
	JMP  m24_32768_stage2_inner

m24_32768_stage2_next:
	ADDQ $16, CX
	JMP  m24_32768_stage2_outer

	// ==================================================================
	// Stage 3: 512 groups, each with 16 butterflies
	// Twiddle step = 512
	// ==================================================================
m24_32768_stage3:
	XORQ CX, CX

m24_32768_stage3_outer:
	CMPQ CX, $32768
	JGE  m24_32768_stage4

	XORQ DX, DX

m24_32768_stage3_inner:
	CMPQ DX, $16
	JGE  m24_32768_stage3_next

	MOVQ CX, BX
	ADDQ DX, BX
	LEAQ 16(BX), SI
	LEAQ 32(BX), DI
	LEAQ 48(BX), R14

	// Twiddles: j*512, 2*j*512, 3*j*512
	MOVQ DX, R15
	SHLQ $9, R15
	MOVQ R15, AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X8
	LEAQ (R15)(R15*1), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X9
	LEAQ (R15)(R15*2), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X10

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X0
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X1
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X2
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X3

	// Complex multiply a1*w1, a2*w2, a3*w3
	VMOVDDUP X8, X11
	VPERMILPD $1, X8, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X1, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X1, X13
	VMOVAPD X13, X1

	VMOVDDUP X9, X11
	VPERMILPD $1, X9, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X2, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X2, X13
	VMOVAPD X13, X2

	VMOVDDUP X10, X11
	VPERMILPD $1, X10, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X3, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X3, X13
	VMOVAPD X13, X3

	VADDPD X0, X2, X4
	VSUBPD X2, X0, X5
	VADDPD X1, X3, X6
	VSUBPD X3, X1, X7

	VSHUFPD $0x1, X7, X7, X14
	VXORPD ·maskNegHiPD(SB), X14, X14

	VSHUFPD $0x1, X7, X7, X12
	VXORPD ·maskNegLoPD(SB), X12, X12

	VADDPD X4, X6, X0
	VADDPD X5, X14, X1
	VSUBPD X6, X4, X2
	VADDPD X5, X12, X3

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD X0, (R8)(AX*1)
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD X1, (R8)(AX*1)
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD X2, (R8)(AX*1)
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD X3, (R8)(AX*1)

	INCQ DX
	JMP  m24_32768_stage3_inner

m24_32768_stage3_next:
	ADDQ $64, CX
	JMP  m24_32768_stage3_outer

	// ==================================================================
	// Stage 4: 128 groups, each with 64 butterflies
	// Twiddle step = 128
	// ==================================================================
m24_32768_stage4:
	XORQ CX, CX

m24_32768_stage4_outer:
	CMPQ CX, $32768
	JGE  m24_32768_stage5

	XORQ DX, DX

m24_32768_stage4_inner:
	CMPQ DX, $64
	JGE  m24_32768_stage4_next

	MOVQ CX, BX
	ADDQ DX, BX
	LEAQ 64(BX), SI
	LEAQ 128(BX), DI
	LEAQ 192(BX), R14

	// Twiddles: j*128, 2*j*128, 3*j*128
	MOVQ DX, R15
	SHLQ $7, R15
	MOVQ R15, AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X8
	LEAQ (R15)(R15*1), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X9
	LEAQ (R15)(R15*2), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X10

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X0
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X1
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X2
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X3

	// Complex multiply a1*w1, a2*w2, a3*w3
	VMOVDDUP X8, X11
	VPERMILPD $1, X8, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X1, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X1, X13
	VMOVAPD X13, X1

	VMOVDDUP X9, X11
	VPERMILPD $1, X9, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X2, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X2, X13
	VMOVAPD X13, X2

	VMOVDDUP X10, X11
	VPERMILPD $1, X10, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X3, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X3, X13
	VMOVAPD X13, X3

	VADDPD X0, X2, X4
	VSUBPD X2, X0, X5
	VADDPD X1, X3, X6
	VSUBPD X3, X1, X7

	VSHUFPD $0x1, X7, X7, X14
	VXORPD ·maskNegHiPD(SB), X14, X14

	VSHUFPD $0x1, X7, X7, X12
	VXORPD ·maskNegLoPD(SB), X12, X12

	VADDPD X4, X6, X0
	VADDPD X5, X14, X1
	VSUBPD X6, X4, X2
	VADDPD X5, X12, X3

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD X0, (R8)(AX*1)
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD X1, (R8)(AX*1)
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD X2, (R8)(AX*1)
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD X3, (R8)(AX*1)

	INCQ DX
	JMP  m24_32768_stage4_inner

m24_32768_stage4_next:
	ADDQ $256, CX
	JMP  m24_32768_stage4_outer

	// ==================================================================
	// Stage 5: 32 groups, each with 256 butterflies
	// Twiddle step = 32
	// ==================================================================
m24_32768_stage5:
	XORQ CX, CX

m24_32768_stage5_outer:
	CMPQ CX, $32768
	JGE  m24_32768_stage6

	XORQ DX, DX

m24_32768_stage5_inner:
	CMPQ DX, $256
	JGE  m24_32768_stage5_next

	MOVQ CX, BX
	ADDQ DX, BX
	LEAQ 256(BX), SI
	LEAQ 512(BX), DI
	LEAQ 768(BX), R14

	// Twiddles: j*32, 2*j*32, 3*j*32
	MOVQ DX, R15
	SHLQ $5, R15
	MOVQ R15, AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X8
	LEAQ (R15)(R15*1), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X9
	LEAQ (R15)(R15*2), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X10

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X0
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X1
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X2
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X3

	// Complex multiply a1*w1, a2*w2, a3*w3
	VMOVDDUP X8, X11
	VPERMILPD $1, X8, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X1, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X1, X13
	VMOVAPD X13, X1

	VMOVDDUP X9, X11
	VPERMILPD $1, X9, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X2, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X2, X13
	VMOVAPD X13, X2

	VMOVDDUP X10, X11
	VPERMILPD $1, X10, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X3, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X3, X13
	VMOVAPD X13, X3

	VADDPD X0, X2, X4
	VSUBPD X2, X0, X5
	VADDPD X1, X3, X6
	VSUBPD X3, X1, X7

	VSHUFPD $0x1, X7, X7, X14
	VXORPD ·maskNegHiPD(SB), X14, X14

	VSHUFPD $0x1, X7, X7, X12
	VXORPD ·maskNegLoPD(SB), X12, X12

	VADDPD X4, X6, X0
	VADDPD X5, X14, X1
	VSUBPD X6, X4, X2
	VADDPD X5, X12, X3

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD X0, (R8)(AX*1)
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD X1, (R8)(AX*1)
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD X2, (R8)(AX*1)
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD X3, (R8)(AX*1)

	INCQ DX
	JMP  m24_32768_stage5_inner

m24_32768_stage5_next:
	ADDQ $1024, CX
	JMP  m24_32768_stage5_outer

	// ==================================================================
	// Stage 6: 8 groups, each with 1024 butterflies
	// Twiddle step = 8
	// ==================================================================
m24_32768_stage6:
	XORQ CX, CX

m24_32768_stage6_outer:
	CMPQ CX, $32768
	JGE  m24_32768_stage7

	XORQ DX, DX

m24_32768_stage6_inner:
	CMPQ DX, $1024
	JGE  m24_32768_stage6_next

	MOVQ CX, BX
	ADDQ DX, BX
	LEAQ 1024(BX), SI
	LEAQ 2048(BX), DI
	LEAQ 3072(BX), R14

	// Twiddles: j*8, 2*j*8, 3*j*8
	MOVQ DX, R15
	SHLQ $3, R15
	MOVQ R15, AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X8
	LEAQ (R15)(R15*1), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X9
	LEAQ (R15)(R15*2), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X10

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X0
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X1
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X2
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X3

	// Complex multiply a1*w1, a2*w2, a3*w3
	VMOVDDUP X8, X11
	VPERMILPD $1, X8, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X1, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X1, X13
	VMOVAPD X13, X1

	VMOVDDUP X9, X11
	VPERMILPD $1, X9, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X2, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X2, X13
	VMOVAPD X13, X2

	VMOVDDUP X10, X11
	VPERMILPD $1, X10, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X3, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X3, X13
	VMOVAPD X13, X3

	VADDPD X0, X2, X4
	VSUBPD X2, X0, X5
	VADDPD X1, X3, X6
	VSUBPD X3, X1, X7

	VSHUFPD $0x1, X7, X7, X14
	VXORPD ·maskNegHiPD(SB), X14, X14

	VSHUFPD $0x1, X7, X7, X12
	VXORPD ·maskNegLoPD(SB), X12, X12

	VADDPD X4, X6, X0
	VADDPD X5, X14, X1
	VSUBPD X6, X4, X2
	VADDPD X5, X12, X3

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD X0, (R8)(AX*1)
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD X1, (R8)(AX*1)
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD X2, (R8)(AX*1)
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD X3, (R8)(AX*1)

	INCQ DX
	JMP  m24_32768_stage6_inner

m24_32768_stage6_next:
	ADDQ $4096, CX
	JMP  m24_32768_stage6_outer

	// ==================================================================
	// Stage 7: 2 groups, each with 4096 butterflies
	// Twiddle step = 2
	// ==================================================================
m24_32768_stage7:
	XORQ CX, CX

m24_32768_stage7_outer:
	CMPQ CX, $32768
	JGE  m24_32768_stage8

	XORQ DX, DX

m24_32768_stage7_inner:
	CMPQ DX, $4096
	JGE  m24_32768_stage7_next

	MOVQ CX, BX
	ADDQ DX, BX
	LEAQ 4096(BX), SI
	LEAQ 8192(BX), DI
	LEAQ 12288(BX), R14

	// Twiddles: j*2, 2*j*2, 3*j*2
	MOVQ DX, R15
	SHLQ $1, R15
	MOVQ R15, AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X8
	LEAQ (R15)(R15*1), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X9
	LEAQ (R15)(R15*2), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X10

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X0
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X1
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X2
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X3

	// Complex multiply a1*w1, a2*w2, a3*w3
	VMOVDDUP X8, X11
	VPERMILPD $1, X8, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X1, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X1, X13
	VMOVAPD X13, X1

	VMOVDDUP X9, X11
	VPERMILPD $1, X9, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X2, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X2, X13
	VMOVAPD X13, X2

	VMOVDDUP X10, X11
	VPERMILPD $1, X10, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X3, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X3, X13
	VMOVAPD X13, X3

	VADDPD X0, X2, X4
	VSUBPD X2, X0, X5
	VADDPD X1, X3, X6
	VSUBPD X3, X1, X7

	VSHUFPD $0x1, X7, X7, X14
	VXORPD ·maskNegHiPD(SB), X14, X14

	VSHUFPD $0x1, X7, X7, X12
	VXORPD ·maskNegLoPD(SB), X12, X12

	VADDPD X4, X6, X0
	VADDPD X5, X14, X1
	VSUBPD X6, X4, X2
	VADDPD X5, X12, X3

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD X0, (R8)(AX*1)
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD X1, (R8)(AX*1)
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD X2, (R8)(AX*1)
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD X3, (R8)(AX*1)

	INCQ DX
	JMP  m24_32768_stage7_inner

m24_32768_stage7_next:
	ADDQ $16384, CX
	JMP  m24_32768_stage7_outer

	// ==================================================================
	// Stage 8: radix-2 final stage
	// ==================================================================
m24_32768_stage8:
	XORQ CX, CX

m24_32768_stage8_loop:
	CMPQ CX, $16384
	JGE  m24_32768_forward_done

	MOVQ CX, BX
	LEAQ 16384(BX), SI

	MOVQ CX, AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X8   // twiddle
	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X0    // a
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X1    // b

	// b = b * w
	VMOVDDUP X8, X11
	VPERMILPD $1, X8, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X1, X13
	VMULPD X12, X13, X13
	VFMADDSUB231PD X11, X1, X13
	VMOVAPD X13, X1

	VADDPD X0, X1, X2
	VSUBPD X1, X0, X3

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD X2, (R8)(AX*1)
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD X3, (R8)(AX*1)

	INCQ CX
	JMP  m24_32768_stage8_loop

m24_32768_forward_done:
	// Copy results to dst if needed
	MOVQ dst+0(FP), R9
	CMPQ R8, R9
	JE   m24_32768_forward_ret

	XORQ CX, CX

m24_32768_copy_loop:
	VMOVUPD (R8)(CX*1), Y0
	VMOVUPD 32(R8)(CX*1), Y1
	VMOVUPD Y0, (R9)(CX*1)
	VMOVUPD Y1, 32(R9)(CX*1)
	ADDQ $64, CX
	CMPQ CX, $524288
	JL   m24_32768_copy_loop

m24_32768_forward_ret:
	VZEROUPPER
	MOVB $1, ret+120(FP)
	RET

m24_32768_return_false:
	VZEROUPPER
	MOVB $0, ret+120(FP)
	RET

TEXT ·InverseAVX2Size32768Radix4Then2Complex128Asm(SB), NOSPLIT, $0-121
	// Load parameters
	MOVQ dst+0(FP), R8       // R8  = dst pointer
	MOVQ src+24(FP), R9      // R9  = src pointer
	MOVQ twiddle+48(FP), R10 // R10 = twiddle pointer
	MOVQ scratch+72(FP), R11 // R11 = scratch pointer
	MOVQ bitrev+96(FP), R12  // R12 = bitrev pointer
	MOVQ src_len+32(FP), R13 // R13 = n (should be 32768)

	// Verify n == 32768
	CMPQ R13, $32768
	JNE  m24_32768_inv_return_false

	// Validate slice lengths
	MOVQ dst_len+8(FP), AX
	CMPQ AX, $32768
	JL   m24_32768_inv_return_false

	MOVQ twiddle_len+56(FP), AX
	CMPQ AX, $32768
	JL   m24_32768_inv_return_false

	MOVQ scratch_len+80(FP), AX
	CMPQ AX, $32768
	JL   m24_32768_inv_return_false

	MOVQ bitrev_len+104(FP), AX
	CMPQ AX, $32768
	JL   m24_32768_inv_return_false

	// Select working buffer
	CMPQ R8, R9
	JNE  m24_32768_inv_use_dst
	MOVQ R11, R8             // In-place: use scratch

m24_32768_inv_use_dst:
	// ==================================================================
	// Stage 1: 8192 radix-4 butterflies with radix-4-then-2 bit-reversal
	// ==================================================================
	XORQ CX, CX              // CX = base offset

m24_32768_inv_stage1_loop:
	CMPQ CX, $32768
	JGE  m24_32768_inv_stage2

	// Load bit-reversed indices
	MOVQ (R12)(CX*8), DX
	MOVQ 8(R12)(CX*8), SI
	MOVQ 16(R12)(CX*8), DI
	MOVQ 24(R12)(CX*8), R14

	// Load input values
	MOVQ DX, AX
	SHLQ $4, AX
	VMOVUPD (R9)(AX*1), X0
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R9)(AX*1), X1
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD (R9)(AX*1), X2
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD (R9)(AX*1), X3

	// Radix-4 butterfly
	VADDPD X0, X2, X4
	VSUBPD X2, X0, X5
	VADDPD X1, X3, X6
	VSUBPD X3, X1, X7

	// i*t3 for y1
	VSHUFPD $0x1, X7, X7, X8
	VXORPD ·maskNegLoPD(SB), X8, X8

	// (-i)*t3 for y3
	VSHUFPD $0x1, X7, X7, X11
	VXORPD ·maskNegHiPD(SB), X11, X11

	VADDPD X4, X6, X0
	VADDPD X5, X8, X1
	VSUBPD X6, X4, X2
	VADDPD X5, X11, X3

	// Store outputs to work buffer
	MOVQ CX, AX
	SHLQ $4, AX
	VMOVUPD X0, (R8)(AX*1)
	VMOVUPD X1, 16(R8)(AX*1)
	VMOVUPD X2, 32(R8)(AX*1)
	VMOVUPD X3, 48(R8)(AX*1)

	ADDQ $4, CX
	JMP  m24_32768_inv_stage1_loop

	// ==================================================================
	// Stage 2: 2048 groups, each with 4 butterflies
	// Twiddle step = 2048 (conjugated twiddles)
	// ==================================================================
m24_32768_inv_stage2:
	XORQ CX, CX

m24_32768_inv_stage2_outer:
	CMPQ CX, $32768
	JGE  m24_32768_inv_stage3

	XORQ DX, DX

m24_32768_inv_stage2_inner:
	CMPQ DX, $4
	JGE  m24_32768_inv_stage2_next

	MOVQ CX, BX
	ADDQ DX, BX
	LEAQ 4(BX), SI
	LEAQ 8(BX), DI
	LEAQ 12(BX), R14

	// Twiddles: j*2048, 2*j*2048, 3*j*2048
	MOVQ DX, R15
	SHLQ $11, R15
	MOVQ R15, AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X8
	LEAQ (R15)(R15*1), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X9
	LEAQ (R15)(R15*2), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X10

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X0
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X1
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X2
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X3

	// Complex multiply a1*conj(w)1, a2*conj(w)2, a3*conj(w)3
	VMOVDDUP X8, X11
	VPERMILPD $1, X8, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X1, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X1, X13
	VMOVAPD X13, X1

	VMOVDDUP X9, X11
	VPERMILPD $1, X9, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X2, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X2, X13
	VMOVAPD X13, X2

	VMOVDDUP X10, X11
	VPERMILPD $1, X10, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X3, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X3, X13
	VMOVAPD X13, X3

	VADDPD X0, X2, X4
	VSUBPD X2, X0, X5
	VADDPD X1, X3, X6
	VSUBPD X3, X1, X7

	VSHUFPD $0x1, X7, X7, X14
	VXORPD ·maskNegLoPD(SB), X14, X14

	VSHUFPD $0x1, X7, X7, X12
	VXORPD ·maskNegHiPD(SB), X12, X12

	VADDPD X4, X6, X0
	VADDPD X5, X14, X1
	VSUBPD X6, X4, X2
	VADDPD X5, X12, X3

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD X0, (R8)(AX*1)
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD X1, (R8)(AX*1)
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD X2, (R8)(AX*1)
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD X3, (R8)(AX*1)

	INCQ DX
	JMP  m24_32768_inv_stage2_inner

m24_32768_inv_stage2_next:
	ADDQ $16, CX
	JMP  m24_32768_inv_stage2_outer

	// ==================================================================
	// Stage 3: 512 groups, each with 16 butterflies
	// Twiddle step = 512 (conjugated twiddles)
	// ==================================================================
m24_32768_inv_stage3:
	XORQ CX, CX

m24_32768_inv_stage3_outer:
	CMPQ CX, $32768
	JGE  m24_32768_inv_stage4

	XORQ DX, DX

m24_32768_inv_stage3_inner:
	CMPQ DX, $16
	JGE  m24_32768_inv_stage3_next

	MOVQ CX, BX
	ADDQ DX, BX
	LEAQ 16(BX), SI
	LEAQ 32(BX), DI
	LEAQ 48(BX), R14

	// Twiddles: j*512, 2*j*512, 3*j*512
	MOVQ DX, R15
	SHLQ $9, R15
	MOVQ R15, AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X8
	LEAQ (R15)(R15*1), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X9
	LEAQ (R15)(R15*2), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X10

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X0
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X1
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X2
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X3

	// Complex multiply a1*conj(w)1, a2*conj(w)2, a3*conj(w)3
	VMOVDDUP X8, X11
	VPERMILPD $1, X8, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X1, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X1, X13
	VMOVAPD X13, X1

	VMOVDDUP X9, X11
	VPERMILPD $1, X9, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X2, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X2, X13
	VMOVAPD X13, X2

	VMOVDDUP X10, X11
	VPERMILPD $1, X10, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X3, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X3, X13
	VMOVAPD X13, X3

	VADDPD X0, X2, X4
	VSUBPD X2, X0, X5
	VADDPD X1, X3, X6
	VSUBPD X3, X1, X7

	VSHUFPD $0x1, X7, X7, X14
	VXORPD ·maskNegLoPD(SB), X14, X14

	VSHUFPD $0x1, X7, X7, X12
	VXORPD ·maskNegHiPD(SB), X12, X12

	VADDPD X4, X6, X0
	VADDPD X5, X14, X1
	VSUBPD X6, X4, X2
	VADDPD X5, X12, X3

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD X0, (R8)(AX*1)
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD X1, (R8)(AX*1)
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD X2, (R8)(AX*1)
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD X3, (R8)(AX*1)

	INCQ DX
	JMP  m24_32768_inv_stage3_inner

m24_32768_inv_stage3_next:
	ADDQ $64, CX
	JMP  m24_32768_inv_stage3_outer

	// ==================================================================
	// Stage 4: 128 groups, each with 64 butterflies
	// Twiddle step = 128 (conjugated twiddles)
	// ==================================================================
m24_32768_inv_stage4:
	XORQ CX, CX

m24_32768_inv_stage4_outer:
	CMPQ CX, $32768
	JGE  m24_32768_inv_stage5

	XORQ DX, DX

m24_32768_inv_stage4_inner:
	CMPQ DX, $64
	JGE  m24_32768_inv_stage4_next

	MOVQ CX, BX
	ADDQ DX, BX
	LEAQ 64(BX), SI
	LEAQ 128(BX), DI
	LEAQ 192(BX), R14

	// Twiddles: j*128, 2*j*128, 3*j*128
	MOVQ DX, R15
	SHLQ $7, R15
	MOVQ R15, AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X8
	LEAQ (R15)(R15*1), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X9
	LEAQ (R15)(R15*2), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X10

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X0
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X1
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X2
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X3

	// Complex multiply a1*conj(w)1, a2*conj(w)2, a3*conj(w)3
	VMOVDDUP X8, X11
	VPERMILPD $1, X8, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X1, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X1, X13
	VMOVAPD X13, X1

	VMOVDDUP X9, X11
	VPERMILPD $1, X9, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X2, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X2, X13
	VMOVAPD X13, X2

	VMOVDDUP X10, X11
	VPERMILPD $1, X10, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X3, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X3, X13
	VMOVAPD X13, X3

	VADDPD X0, X2, X4
	VSUBPD X2, X0, X5
	VADDPD X1, X3, X6
	VSUBPD X3, X1, X7

	VSHUFPD $0x1, X7, X7, X14
	VXORPD ·maskNegLoPD(SB), X14, X14

	VSHUFPD $0x1, X7, X7, X12
	VXORPD ·maskNegHiPD(SB), X12, X12

	VADDPD X4, X6, X0
	VADDPD X5, X14, X1
	VSUBPD X6, X4, X2
	VADDPD X5, X12, X3

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD X0, (R8)(AX*1)
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD X1, (R8)(AX*1)
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD X2, (R8)(AX*1)
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD X3, (R8)(AX*1)

	INCQ DX
	JMP  m24_32768_inv_stage4_inner

m24_32768_inv_stage4_next:
	ADDQ $256, CX
	JMP  m24_32768_inv_stage4_outer

	// ==================================================================
	// Stage 5: 32 groups, each with 256 butterflies
	// Twiddle step = 32 (conjugated twiddles)
	// ==================================================================
m24_32768_inv_stage5:
	XORQ CX, CX

m24_32768_inv_stage5_outer:
	CMPQ CX, $32768
	JGE  m24_32768_inv_stage6

	XORQ DX, DX

m24_32768_inv_stage5_inner:
	CMPQ DX, $256
	JGE  m24_32768_inv_stage5_next

	MOVQ CX, BX
	ADDQ DX, BX
	LEAQ 256(BX), SI
	LEAQ 512(BX), DI
	LEAQ 768(BX), R14

	// Twiddles: j*32, 2*j*32, 3*j*32
	MOVQ DX, R15
	SHLQ $5, R15
	MOVQ R15, AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X8
	LEAQ (R15)(R15*1), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X9
	LEAQ (R15)(R15*2), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X10

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X0
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X1
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X2
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X3

	// Complex multiply a1*conj(w)1, a2*conj(w)2, a3*conj(w)3
	VMOVDDUP X8, X11
	VPERMILPD $1, X8, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X1, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X1, X13
	VMOVAPD X13, X1

	VMOVDDUP X9, X11
	VPERMILPD $1, X9, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X2, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X2, X13
	VMOVAPD X13, X2

	VMOVDDUP X10, X11
	VPERMILPD $1, X10, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X3, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X3, X13
	VMOVAPD X13, X3

	VADDPD X0, X2, X4
	VSUBPD X2, X0, X5
	VADDPD X1, X3, X6
	VSUBPD X3, X1, X7

	VSHUFPD $0x1, X7, X7, X14
	VXORPD ·maskNegLoPD(SB), X14, X14

	VSHUFPD $0x1, X7, X7, X12
	VXORPD ·maskNegHiPD(SB), X12, X12

	VADDPD X4, X6, X0
	VADDPD X5, X14, X1
	VSUBPD X6, X4, X2
	VADDPD X5, X12, X3

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD X0, (R8)(AX*1)
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD X1, (R8)(AX*1)
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD X2, (R8)(AX*1)
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD X3, (R8)(AX*1)

	INCQ DX
	JMP  m24_32768_inv_stage5_inner

m24_32768_inv_stage5_next:
	ADDQ $1024, CX
	JMP  m24_32768_inv_stage5_outer

	// ==================================================================
	// Stage 6: 8 groups, each with 1024 butterflies
	// Twiddle step = 8 (conjugated twiddles)
	// ==================================================================
m24_32768_inv_stage6:
	XORQ CX, CX

m24_32768_inv_stage6_outer:
	CMPQ CX, $32768
	JGE  m24_32768_inv_stage7

	XORQ DX, DX

m24_32768_inv_stage6_inner:
	CMPQ DX, $1024
	JGE  m24_32768_inv_stage6_next

	MOVQ CX, BX
	ADDQ DX, BX
	LEAQ 1024(BX), SI
	LEAQ 2048(BX), DI
	LEAQ 3072(BX), R14

	// Twiddles: j*8, 2*j*8, 3*j*8
	MOVQ DX, R15
	SHLQ $3, R15
	MOVQ R15, AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X8
	LEAQ (R15)(R15*1), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X9
	LEAQ (R15)(R15*2), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X10

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X0
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X1
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X2
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X3

	// Complex multiply a1*conj(w)1, a2*conj(w)2, a3*conj(w)3
	VMOVDDUP X8, X11
	VPERMILPD $1, X8, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X1, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X1, X13
	VMOVAPD X13, X1

	VMOVDDUP X9, X11
	VPERMILPD $1, X9, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X2, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X2, X13
	VMOVAPD X13, X2

	VMOVDDUP X10, X11
	VPERMILPD $1, X10, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X3, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X3, X13
	VMOVAPD X13, X3

	VADDPD X0, X2, X4
	VSUBPD X2, X0, X5
	VADDPD X1, X3, X6
	VSUBPD X3, X1, X7

	VSHUFPD $0x1, X7, X7, X14
	VXORPD ·maskNegLoPD(SB), X14, X14

	VSHUFPD $0x1, X7, X7, X12
	VXORPD ·maskNegHiPD(SB), X12, X12

	VADDPD X4, X6, X0
	VADDPD X5, X14, X1
	VSUBPD X6, X4, X2
	VADDPD X5, X12, X3

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD X0, (R8)(AX*1)
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD X1, (R8)(AX*1)
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD X2, (R8)(AX*1)
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD X3, (R8)(AX*1)

	INCQ DX
	JMP  m24_32768_inv_stage6_inner

m24_32768_inv_stage6_next:
	ADDQ $4096, CX
	JMP  m24_32768_inv_stage6_outer

	// ==================================================================
	// Stage 7: 2 groups, each with 4096 butterflies
	// Twiddle step = 2 (conjugated twiddles)
	// ==================================================================
m24_32768_inv_stage7:
	XORQ CX, CX

m24_32768_inv_stage7_outer:
	CMPQ CX, $32768
	JGE  m24_32768_inv_stage8

	XORQ DX, DX

m24_32768_inv_stage7_inner:
	CMPQ DX, $4096
	JGE  m24_32768_inv_stage7_next

	MOVQ CX, BX
	ADDQ DX, BX
	LEAQ 4096(BX), SI
	LEAQ 8192(BX), DI
	LEAQ 12288(BX), R14

	// Twiddles: j*2, 2*j*2, 3*j*2
	MOVQ DX, R15
	SHLQ $1, R15
	MOVQ R15, AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X8
	LEAQ (R15)(R15*1), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X9
	LEAQ (R15)(R15*2), AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X10

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X0
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X1
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X2
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X3

	// Complex multiply a1*conj(w)1, a2*conj(w)2, a3*conj(w)3
	VMOVDDUP X8, X11
	VPERMILPD $1, X8, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X1, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X1, X13
	VMOVAPD X13, X1

	VMOVDDUP X9, X11
	VPERMILPD $1, X9, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X2, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X2, X13
	VMOVAPD X13, X2

	VMOVDDUP X10, X11
	VPERMILPD $1, X10, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X3, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X3, X13
	VMOVAPD X13, X3

	VADDPD X0, X2, X4
	VSUBPD X2, X0, X5
	VADDPD X1, X3, X6
	VSUBPD X3, X1, X7

	VSHUFPD $0x1, X7, X7, X14
	VXORPD ·maskNegLoPD(SB), X14, X14

	VSHUFPD $0x1, X7, X7, X12
	VXORPD ·maskNegHiPD(SB), X12, X12

	VADDPD X4, X6, X0
	VADDPD X5, X14, X1
	VSUBPD X6, X4, X2
	VADDPD X5, X12, X3

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD X0, (R8)(AX*1)
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD X1, (R8)(AX*1)
	MOVQ DI, AX
	SHLQ $4, AX
	VMOVUPD X2, (R8)(AX*1)
	MOVQ R14, AX
	SHLQ $4, AX
	VMOVUPD X3, (R8)(AX*1)

	INCQ DX
	JMP  m24_32768_inv_stage7_inner

m24_32768_inv_stage7_next:
	ADDQ $16384, CX
	JMP  m24_32768_inv_stage7_outer

	// ==================================================================
	// Stage 8: radix-2 final stage (conjugated twiddles)
	// ==================================================================
m24_32768_inv_stage8:
	XORQ CX, CX

m24_32768_inv_stage8_loop:
	CMPQ CX, $16384
	JGE  m24_32768_inv_scale

	MOVQ CX, BX
	LEAQ 16384(BX), SI

	MOVQ CX, AX
	SHLQ $4, AX
	VMOVUPD (R10)(AX*1), X8   // twiddle
	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X0    // a
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD (R8)(AX*1), X1    // b

	// b = b * conj(w)
	VMOVDDUP X8, X11
	VPERMILPD $1, X8, X12
	VMOVDDUP X12, X12
	VPERMILPD $1, X1, X13
	VMULPD X12, X13, X13
	VFMSUBADD231PD X11, X1, X13
	VMOVAPD X13, X1

	VADDPD X0, X1, X2
	VSUBPD X1, X0, X3

	MOVQ BX, AX
	SHLQ $4, AX
	VMOVUPD X2, (R8)(AX*1)
	MOVQ SI, AX
	SHLQ $4, AX
	VMOVUPD X3, (R8)(AX*1)

	INCQ CX
	JMP  m24_32768_inv_stage8_loop

m24_32768_inv_scale:
	// ==================================================================
	// Apply 1/N scaling for inverse transform
	// ==================================================================
	VMOVSD ·oneThirtyTwoThousandSevenHundredSixtyEighth64(SB), X8
	VBROADCASTSD X8, Y8

	XORQ CX, CX

m24_32768_inv_scale_loop:
	VMOVUPD (R8)(CX*1), Y0
	VMOVUPD 32(R8)(CX*1), Y1
	VMULPD Y8, Y0, Y0
	VMULPD Y8, Y1, Y1
	VMOVUPD Y0, (R8)(CX*1)
	VMOVUPD Y1, 32(R8)(CX*1)
	ADDQ $64, CX
	CMPQ CX, $524288
	JL   m24_32768_inv_scale_loop

	// Copy results to dst if needed
	MOVQ dst+0(FP), R9
	CMPQ R8, R9
	JE   m24_32768_inv_done

	XORQ CX, CX

m24_32768_inv_copy_loop:
	VMOVUPD (R8)(CX*1), Y0
	VMOVUPD 32(R8)(CX*1), Y1
	VMOVUPD Y0, (R9)(CX*1)
	VMOVUPD Y1, 32(R9)(CX*1)
	ADDQ $64, CX
	CMPQ CX, $524288
	JL   m24_32768_inv_copy_loop

m24_32768_inv_done:
	VZEROUPPER
	MOVB $1, ret+120(FP)
	RET

m24_32768_inv_return_false:
	VZEROUPPER
	MOVB $0, ret+120(FP)
	RET
